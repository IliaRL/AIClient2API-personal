name: Go Dependency Submission

on:
  push:
    branches:
      - main
    paths:
      - 'Tier1-AIClient2API/tls-sidecar/go.mod'
      - 'Tier1-AIClient2API/tls-sidecar/go.sum'
      - '.github/workflows/dependency-graph-go.yml'
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  go-dependency-submission:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: Tier1-AIClient2API/tls-sidecar/go.mod

      - uses: actions/go-dependency-submission@v2
        with:
          go-mod-path: Tier1-AIClient2API/tls-sidecar/go.mod
          go-build-target: Tier1-AIClient2API/tls-sidecar/main.go
